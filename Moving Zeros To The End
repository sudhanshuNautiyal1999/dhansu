Q1> Write an algorithm that takes an array and moves all of the zeros to the end, preserving the order of the other elements.make function
of name move_zeros
 EXAMPLE-move_zeros([false,1,0,1,2,0,1,3,"a"]) # returns[false,1,1,2,1,3,"a",0,0]
 
 ANSWER
 
 def move_zeros(x):
	a=[]
	z=[]
	for i in x:
		if i!=0:
			z.append(i)
		else:
			a.append(i)
	return z+a
  
